* Following dataset (SQL) contains properly formatted table names and fields
	+ data-InternetSales-PostgreSQL.zip
* Following datasets (SQL and CSV) contain unformatted table names and fields (e.g., uppercase in names)
	+ data-InternetSales-CSV-unformatted.zip
	+ data-InternetSales-PostgreSQL-unformatted.zip